# file: .github/workflows/hexlet-check.yml
name: echo-text
# on Ц определ€ет событи€, которые запускают воркфлоу
on: push
jobs:
  # build Ц произвольно выбранное им€ задани€
  # их может быть больше одного
  build:
    # операционна€ система дл€ работы воркфлоу
    runs-on: ubuntu-latest
    steps: # список шагов, которые надо выполнить
      # экшен Ч выполн€ет какую-то задачу
      # checkout Ч клонирует репозиторий
      - uses: actions/checkout@v3
      # run Ц произвольна€ bash-команда
      # ls -la выведет содержимое текущего репозитори€
      - run: make say-hello